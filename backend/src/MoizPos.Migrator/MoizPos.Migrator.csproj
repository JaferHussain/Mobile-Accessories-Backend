<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <RootNamespace>MoizPos.Migrator</RootNamespace>
    <!--
      Shares the API's secret store so the connection string is configured once, not twice.
      Must stay in sync with backend/src/MoizPos.Api/MoizPos.Api.csproj.
    -->
    <UserSecretsId>4b3e51d4-aa5f-4474-bede-a67d60d16031</UserSecretsId>
  </PropertyGroup>

  <ItemGroup>
    <!-- DbUp discovers migrations as embedded resources, so scripts ship inside the assembly. -->
    <EmbeddedResource Include="Scripts\*.sql" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="dbup-mysql" Version="5.0.41" />
    <PackageReference Include="Microsoft.Extensions.Configuration" Version="8.0.0" />
    <PackageReference Include="Microsoft.Extensions.Configuration.CommandLine" Version="8.0.0" />
    <PackageReference Include="Microsoft.Extensions.Configuration.EnvironmentVariables" Version="8.0.0" />
    <PackageReference Include="Microsoft.Extensions.Configuration.Json" Version="8.0.1" />
    <PackageReference Include="Microsoft.Extensions.Configuration.UserSecrets" Version="8.0.1" />
  </ItemGroup>

</Project>
